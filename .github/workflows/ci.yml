name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
          targets: wasm32-unknown-unknown

      - name: Format
        run: cargo fmt --all -- --check

      - name: Check frost-night-egui no default features
        run: cargo check -p frost-night-egui --no-default-features

      - name: Check workspace
        run: cargo check --workspace

      - name: Check all features
        run: cargo check -p frost-night-egui --all-features

      - name: Check web demo wasm
        run: cargo check -p web-demo --target wasm32-unknown-unknown

      - name: Check storybook wasm
        run: cargo check -p ui-storybook --target wasm32-unknown-unknown

      - name: Clippy
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings
